language: csharp
branches:
  only:
    - master
install:
  - curl -L -o nuget.exe https://dist.nuget.org/win-x86-commandline/latest/nuget.exe
script:
  - mkdir build
  - bash update.sh
  - mono nuget.exe pack ./build/Genocs.WebApiTemplate.nuspec -NoDefaultExcludes -Version 1.0.0
  - mono nuget.exe setApiKey $NUGET_API_KEY -Source $NUGET_SOURCE
  - mono nuget.exe push Genocs.WebApiTemplate.*.nupkg -Source $NUGET_SOURCE
notifications:
  email:
    on_success: never
    on_failure: always
